DB_USER=geoflow_user
DB_PASSWORD=wiwis_gn25889
DB_HOST=localhost
DB_PORT=3306
DB_NAME=geoflow_db




# ==========================
# 🔹 Notas
# ==========================
# DATABASE_URL=mysql+pymysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
# DATABASE_URL=mysql+pymysql://${geoflow_user}:${wiwis_gn25889}@${3306}:${3306}/${geoflow_db}

# 1. En desarrollo local, puedes usar DB_USER, DB_PASSWORD, etc. por separado.
# 2. En producción (Railway, Heroku), generalmente te pedirán DATABASE_URL.
# 3. Nunca subas este archivo a GitHub con tus credenciales reales.




SECRET_KEY=mi_clave_secreta_super_segura
# Es una clave secreta que solo tu backend conoce.
# Se usa para firmar y verificar tokens de acceso (como JWT, JSON Web Tokens).
# Sin esta clave, nadie puede generar o validar tokens válidos.
# Debe ser larga y aleatoria, por ejemplo: kfj39fj3#4F!aI29fN#9.
# ⚠️ Nunca la compartas ni la subas a GitHub.


ALGORITHM=HS256

# Define el algoritmo de encriptación usado para los tokens JWT.
# HS256 significa HMAC-SHA256, un algoritmo seguro y rápido para firmar tokens.
# Otros ejemplos: RS256 (usa claves públicas y privadas).



ACCESS_TOKEN_EXPIRE_MINUTES=30
# Determina cuánto tiempo dura un token antes de expirar.
# En este ejemplo: 30 minutos.
# Después de ese tiempo, el usuario necesitará re-autenticarse para obtener un nuevo token.
# Esto ayuda a reducir riesgos de seguridad si un token se filtra.




# 🔄 Cómo se usan juntos
# Cuando un usuario inicia sesión correctamente:
# Tu backend crea un JWT firmado con SECRET_KEY y ALGORITHM.
# Incluye información como el ID del usuario y la expiración (ACCESS_TOKEN_EXPIRE_MINUTES).
# Cada vez que el frontend hace una petición protegida:
# Envía el token JWT en el header.
# Tu backend lo verifica usando SECRET_KEY y ALGORITHM.
# Si es válido y no expiró, permite la acción; si no, devuelve error de autenticación.



# 💡 En resumen:
# Estas tres líneas configuran la seguridad del sistema de login 
# basado en tokens, no la contraseña de MySQL.
# SECRET_KEY → firma los tokens
# ALGORITHM → algoritmo de firma
# ACCESS_TOKEN_EXPIRE_MINUTES → tiempo de expiración del token